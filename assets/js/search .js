
(function ($) {
    "use strict";

    
   let index = document.querySelector('form-control');
    let index = filter["موتوسيكلات", "سيارات جديده","سيارات مستعملة" , "قوارب"];

    function search_filter(array)
    {
        return index.filter(element => {
            for (let index = 0; index < array.length; index++) {
                const element = array[index];
                if (typeof index.value == "string") {
                    return index[i]
                }
            }
        })
    
    }

}
    (jQuery));